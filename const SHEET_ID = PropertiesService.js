const SHEET_ID = PropertiesService.getScriptProperties().getProperty('SHEET_ID');
const SHEET_NAME = PropertiesService.getScriptProperties().getProperty('SHEET_NAME');

function addLeadToSheet(sheet, leadData) {
  // Implementation to add lead data to the sheet
  // Return the row ID
}

function validateAndSanitizeData(formResponse) {
  // Implementation to validate and sanitize data
}

function getEmailTemplate(templateName) {
  // Implementation to retrieve email template
}

function generateUnsubscribeLink(email) {
  // Implementation to generate unsubscribe link
}
